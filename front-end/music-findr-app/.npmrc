# Use the official npm registry
registry=https://registry.npmjs.org/

# Always save exact versions when installing packages
save-exact=true

# Disable strict SSL (only if you have SSL issues, not recommended)
# strict-ssl=false
